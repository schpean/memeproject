// This file re-exports everything from utils/config.js
// to maintain compatibility with existing imports while avoiding code duplication.
// All configuration settings are now maintained in utils/config.js

export * from '../utils/config';
